# Bevy Engine Migration Tool - Python Dependencies
# Requirements for migrating Bevy projects from version 0.15 to 0.18

# Core dependencies for AST processing and code transformation
ast-grep>=0.12.0  # AST-based code search and transformation tool

# File system operations and path handling
pathlib2>=2.3.7; python_version < "3.4"  # Enhanced pathlib for older Python versions

# YAML processing for configuration files
PyYAML>=6.0.1  # YAML parser and emitter for Python

# Regular expressions and pattern matching (built-in, but listing for completeness)
# re - Built-in Python module

# Subprocess management (built-in, but enhanced version available)
subprocess32>=3.5.4; python_version < "3.2"  # Backport of subprocess for older Python

# Logging and debugging utilities (built-in, but enhanced formatters)
colorlog>=6.7.0  # Colored logging output for better readability

# File operations and utilities
shutil>=3.0.0  # High-level file operations (built-in)

# Date and time handling (built-in)
# datetime - Built-in Python module

# JSON processing (built-in)
# json - Built-in Python module

# Data structures and utilities
dataclasses>=0.8; python_version < "3.7"  # Backport of dataclasses for older Python

# Type hints and annotations
typing-extensions>=4.7.1  # Extended type hints for better code quality

# File pattern matching and globbing (built-in, but enhanced version)
fnmatch>=1.0.0  # Unix filename pattern matching (built-in)

# System and platform utilities (built-in)
# platform - Built-in Python module
# os - Built-in Python module

# Optional dependencies for enhanced functionality
psutil>=5.9.5  # Cross-platform system and process utilities (alternative to resource module)

# Development and testing dependencies (optional)
pytest>=7.4.0  # Testing framework
pytest-cov>=4.1.0  # Coverage plugin for pytest
black>=23.7.0  # Code formatter
flake8>=6.0.0  # Linting tool
mypy>=1.5.0  # Static type checker

# Documentation dependencies (optional)
sphinx>=7.1.0  # Documentation generator
sphinx-rtd-theme>=1.3.0  # Read the Docs theme for Sphinx

# Performance monitoring (optional)
memory-profiler>=0.61.0  # Memory usage profiler
line-profiler>=4.1.0  # Line-by-line profiler

# Configuration file handling
toml>=0.10.2  # TOML parser for Cargo.toml files
configparser>=5.3.0  # Configuration file parser (built-in, but enhanced version)

# Text processing and diff utilities
difflib>=1.0.0  # Text diff utilities (built-in)

# Temporary file handling (built-in)
# tempfile - Built-in Python module

# Error handling and exceptions (built-in)
# traceback - Built-in Python module

# Threading and concurrency (built-in, but enhanced versions available)
# threading - Built-in Python module
# concurrent.futures - Built-in Python module

# Network utilities (for potential future features)
requests>=2.31.0  # HTTP library for potential remote features

# Compression utilities (for backup functionality)
# zipfile - Built-in Python module
# tarfile - Built-in Python module

# Security and hashing (built-in)
# hashlib - Built-in Python module

# Environment variable handling (built-in)
# os.environ - Built-in functionality

# Command line argument parsing (built-in)
# argparse - Built-in Python module

# Logging configuration (built-in)
# logging - Built-in Python module

# File watching (optional, for development)
watchdog>=3.0.0  # File system event monitoring

# Progress bars and user feedback
tqdm>=4.66.0  # Progress bars for long-running operations

# Rich text and console output
rich>=13.5.0  # Rich text and beautiful formatting in terminal

# Validation and schema checking
jsonschema>=4.19.0  # JSON schema validation
cerberus>=1.3.4  # Lightweight data validation library

# Caching utilities
diskcache>=5.6.3  # Disk-based caching for performance

# Parallel processing utilities
joblib>=1.3.0  # Lightweight pipelining and parallel computing

# String utilities and text processing
Levenshtein>=0.21.1  # Fast string matching and similarity
fuzzywuzzy>=0.18.0  # Fuzzy string matching

# File format detection
python-magic>=0.4.27  # File type identification using libmagic

# Cross-platform compatibility utilities
# Note: Most cross-platform functionality is handled by built-in modules
# and the code is designed to work on Windows, macOS, and Linux

# Version constraints for Python compatibility
# This project supports Python 3.7+ for modern features while maintaining compatibility
# Minimum Python version: 3.7 (for dataclasses, typing improvements, and pathlib enhancements)